extends layout

block body
  .navbar.navbar-default.navbar-fixed-top(role="navigation")
    .container
      .navbar-header
        button.navbar-toggle(type="button", data-toggle="collapse", data-target=".navbar-ex1-collapse")
          span.sr-only Toggle navigation
          span.icon-bar
          span.icon-bar
          span.icon-bar
        a.navbar-brand(href="/") Brand
      .collapse.navbar-collapse.navbar-ex1-collapse
        ul(ng-controller="TopMenuCtrl").nav.navbar-nav.navbar-right
          //{{logout}}
          li.dropdown
            a.dropdown-toggle(href="#", data-toggle="dropdown") {{menuOptions}} 
              b.caret
            ul.dropdown-menu
              li.dropdown(ng-repeat="item in listItems")
                a(href="{{item.link}}") {{item.name}}

  .container      
      div(ng-view)
    hr  
    div
      | Angular seed app: v
      span(app-version)
